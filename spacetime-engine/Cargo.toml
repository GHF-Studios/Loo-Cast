[package]
name = "spacetime-engine"
version.workspace = true
edition.workspace = true

[dependencies]
spacetime-engine-macros = { path = "../spacetime-engine-macros" }
bevy = {
    version = "0.19.1",
    default-features = false,
    features = [
        "default_app",
        "default_platform",
        "3d_bevy_render",
        "ui_bevy_render",
        "debug",
    ],
}
avian3d = "0.7.0"
inventory = { workspace = true }
