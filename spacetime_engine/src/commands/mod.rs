// Data types
pub mod structs;

// Functions

// Integrations

// Miscelaneous

use bevy::prelude::*;

pub(in crate) struct CommandsPlugin;

impl Plugin for CommandsPlugin {
    fn build(&self, app: &mut App) {
    }
}