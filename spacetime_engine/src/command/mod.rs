// Data types
pub mod structs;

// Functions

// Integrations

// Miscelaneous

use bevy::prelude::*;

pub(in crate) struct CommandPlugin;

impl Plugin for CommandPlugin {
    fn build(&self, app: &mut App) {
    }
}