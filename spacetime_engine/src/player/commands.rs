use bevy::prelude::*;
use crate::entity::structs::EntityPosition;



// When we upgrade an entity to a Player, we do the following:
// 1. We spawn and setup the following entities:
//      - PlaCamera
// 2. We upgrade to the following bundles & components:
//      - SpriteBundle
//      - ChunkActor
//      - ChunkLoader


pub(crate) async fn pre_startup() {
    
}

pub(crate) async fn startup() {

}

pub(crate) async fn post_startup() {

}