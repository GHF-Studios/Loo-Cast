[package]
name = "base_mod"
version = "0.1.0"
edition = "2021"

[features]
default = []
init_api = []

[lib]
crate-type = ["cdylib"]

[dependencies]
core_mod_api = { path = "../core_mod_api" }
base_mod_macros = { path = "../base_mod_macros" }
