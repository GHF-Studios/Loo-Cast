# base_mod_api — Gameplay scripting bindings

`base_mod_api` provides scripting wrappers and a translation layer (rhai-compatible) for gameplay scripts to safely interact with engine systems. It commonly exposes lightweight helpers and type conversions.

## Guidance

- Place content declarations that use these bindings in explicit content-domain roots under `base_mod/assets/`.
