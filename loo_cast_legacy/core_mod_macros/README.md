# core_mod_macros

`core_mod_macros` is an integral workspace crate. In the current organization pass it is intentionally
unused, while active procedural macro implementations live in `core_engine_macros`.
