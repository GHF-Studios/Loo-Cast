[package]
name = "core_engine"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "core_engine"
path = "src/main.rs"

[dependencies]
core_mod = { path = "../core_mod", features = ["init_api", "allow_premium_box"] }
libloading = "0.8.8"
