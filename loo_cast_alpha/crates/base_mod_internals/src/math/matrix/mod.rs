/// Matrix-focused aliases, including USF/normal union wrappers.
pub mod aliases;
/// Normal matrix representations (generic + concrete glam/bevy-backed).
pub mod normal;
/// Shared matrix contracts consumed by both normal and USF matrix surfaces.
pub mod shared;
/// USF matrix representations and cross-scale matrix contracts.
pub mod usf;
