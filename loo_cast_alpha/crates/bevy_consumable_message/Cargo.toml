[package]
edition = "2024"
name = "bevy_consumable_message"
version = "0.1.0"

[dependencies]
bevy_app = { version = "0.18.0", optional = true }
bevy_ecs = { version = "0.18.0", default-features = false }

[features]
default = ["bevy_app"]

