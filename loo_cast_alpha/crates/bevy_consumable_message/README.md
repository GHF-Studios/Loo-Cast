# bevy_consumable_message — Consumable message utility

Small utility crate providing `ConsumableMessages`, a message collection where messages can be read multiple times but consumed once.
Useful as a compact pattern for ordered, single-consumption events across systems.

See `src/lib.rs` and `src/app.rs` for proper docs and usage examples.