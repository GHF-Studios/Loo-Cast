# Migrations Draft

This file captures migration-impact notes during the current structural-churn phase.
It exists so compatibility-impact thinking is not lost before stable-contract mode is active.

Current phase note:

- Structural contract changes are expected.
- Formal, versioned migration guides are not mandatory yet.
- Use this file for provisional migration notes, risks, and rough upgrade direction.

Promotion path:

- Once stable-contract mode is declared, move concrete per-version migration guides into
  `docs/migrations/<from>-to-<to>.md` and treat that path as authoritative.
