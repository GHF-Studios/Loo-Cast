

// Atomic / fundamental aspects
pub struct Mass;
pub struct Charge;
pub struct Spin;
pub struct Momentum;
pub struct Position;
pub struct Velocity;
pub struct Energy;
pub struct Time;
pub struct Temperature;

// Structural / derived physical aspects
pub struct Volume;
pub struct Density;
pub struct Pressure;
pub struct Entropy;
pub struct Rigidity;
pub struct Elasticity;
pub struct Viscosity;
pub struct Phase; // (solid, liquid, gas, plasma, etc.)

// Interaction-oriented
pub struct Gravity;
pub struct Magnetism;
pub struct ElectricField;
pub struct Radiation;
pub struct QuantumField;
pub struct WaveFunction;
pub struct Bonding; // chemical/atomic

// Emergent / behavioral
pub struct FluidBehavior;
pub struct Turbulence;
pub struct Reaction;
pub struct Fracture;
pub struct Growth;
pub struct Decay;

// Scale-local semantics
pub struct Cohesion;    // (macro)
pub struct Curvature;   // (relativistic)
pub struct Superposition; // (quantum)
pub struct Consciousness; // (macro + above)

// Organizational / metadata-ish
pub struct Composition; // element makeup
pub struct State;       // internal state of an entity or system
pub struct FieldValue;  // placeholder for external field effect
