#![allow(dead_code)]

/// Normal geometry surfaces (render/projection/view-facing).
pub mod normal;
/// Shared geometry-surface contracts.
pub mod shared;
/// USF geometry surfaces (cross-scale/render-domain).
pub mod usf;
