// Data types

// Functions

// Integrations
pub mod operations;

// Miscelaneous

use bevy::prelude::*;

pub(in crate) struct SpriteBundlePlugin;

impl Plugin for SpriteBundlePlugin {
    fn build(&self, _app: &mut App) {
    }
}