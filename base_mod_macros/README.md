# base_mod_macros — Macros for gameplay bindings

Small set of procedural macros aimed at reducing boilerplate in `base_mod_api` wrapper generation and script-related helpers.

Guidance

- Keep macros documented, with examples and unit tests that mirror common script inputs.
- Treat these macros as internal helpers unless they are explicitly promoted for third-party usage.