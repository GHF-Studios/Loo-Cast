[package]
name = "core_mod"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
core_api = { path = "../core_api" }
core_runtime_api = { path = "../core_runtime_api" }
core_mod_macros = { path = "../core_mod_macros" }
