fn main(world, params) {
    // Dedicated zone pre-update stage.
    // Keep this hook separate so zone-oriented script logic has its own file surface.
    // `params` is a typed ZoneHookParams surface.
}
