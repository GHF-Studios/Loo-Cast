[package]
edition = "2021"
name = "bevy_consumable_event"
version = "0.6.0"

[dependencies]
bevy_app = { version = "0.16.1", optional = true }
bevy_ecs = { version = "0.16.1", default-features = false }

[features]
default = ["bevy_app"]

